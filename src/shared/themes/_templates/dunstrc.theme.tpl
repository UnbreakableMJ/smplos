# dunst theme colors -- auto-generated from colors.toml
[global]
    frame_color = "{{ accent }}"

[urgency_low]
    background = "{{ color0 }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ color8 }}"

[urgency_normal]
    background = "{{ background }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ accent }}"

[urgency_critical]
    background = "{{ color1 }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ color1 }}"
